# Zombie Event Shop

Темний Next.js-магазин для Minecraft Java 1.20.1 івенту "Зомбі проти людей". Вхід на івент безкоштовний, а набори й ресурси купуються за внутрішню валюту сайту — талери. Баланс поповнюється вручну: гравець створює заявку, оплачує суму й пише свій Minecraft-нік із сайту в коментарі платежу. Покупки доступні 24/7, а видача відбувається після старту стріму та відкриття сервера. Проєкт не використовує офіційні логотипи Minecraft, Mojang або Microsoft.

## Стек

- Frontend: Next.js App Router, TypeScript, Tailwind CSS.
- Backend: Next.js API routes.
- DB: Prisma + SQLite для локального тесту.
- Payment: manual top-up requests with admin-issued talers.
- Admin: пароль + HttpOnly session cookie.

## Запуск

1. Встановіть залежності:

```bash
npm install
```

2. Скопіюйте env:

```bash
cp .env.example .env
```

3. Створіть SQLite базу й засійте товари:

```bash
npm run db:push
npm run db:seed
```

4. Запустіть сайт:

```bash
npm run dev
```

Сайт відкриється на `http://localhost:3000`.

## Ручне поповнення балансу

Автоматична оплата тимчасово вимкнена. Коли гравець натискає "Створити заявку", сайт створює pending-поповнення, показує суму, Minecraft-нік із сайту як коментар до платежу й попереджає, що талери видаються вручну протягом 24 годин.

Якщо хочете показувати кнопку переходу до оплати, додайте посилання у `.env` або Environment Variables на Vercel:

```env
APP_URL="https://ваш-домен.example"
MANUAL_TOP_UP_PAYMENT_URL="https://send.monobank.ua/jar/..."
```

Значення `MANUAL_TOP_UP_PAYMENT_URL` необовʼязкове. Якщо воно порожнє, сайт усе одно створить заявку й покаже суму та коментар; реквізити можна передати гравцям окремо.

Адмінський сценарій:

1. Гравець створює заявку на `/top-up`.
2. Гравець оплачує точну суму й пише свій нік у коментарі.
3. Адмін відкриває `/admin`, знаходить гравця за ніком, перевіряє pending-поповнення.
4. Адмін натискає "Видати талери" біля потрібного поповнення. Баланс гравця оновиться, а поповнення стане виданим.

## Адмін-панель

Адмін-панель доступна на `/admin`.

Посилання на адмінку приховане з навігації та футера. Відкривати її потрібно напряму.

Пароль задається в `.env`:

```env
ADMIN_PASSWORD="надійний-пароль"
ADMIN_SESSION_SECRET="довгий-випадковий-секрет"
USER_SESSION_SECRET="інший-довгий-випадковий-секрет"
```

Функції:

- перегляд усіх замовлень;
- фільтри `pending`, `paid`, `issued`, `failed`, `cancelled`;
- показ Minecraft-ніка, контакту, товарів, суми в талерах, статусу й дати;
- готові `/give` команди з підставленим ніком;
- кнопка "Позначити як видано" для замовлень, що очікують видачі;
- кнопка "Скасувати" для замовлень, які ще не видані;
- пошук гравця за ніком або email;
- перегляд балансу, поповнень, покупок і руху талерів;
- видача pending-поповнень після ручної перевірки платежу;
- ручне додавання або списання талерів;
- перемикач статусу стріму для ручної видачі ресурсів;
- додавання, редагування й вимикання товарів.

Покупки доступні 24/7. Перемикач стріму не блокує замовлення, а лише показує адміну, що сервер відкритий для видачі після старту стріму.

## Категорії товарів

- Базові набори
- Шалкери виживання
- Шалкери бою
- Суперпредмети
- Супер ресурси

Квитків і косметики в каталозі немає: вхід безкоштовний, а косметичні привілеї не використовуються. Супер ресурси продаються варіантами x16 / x32 / x64, мінімальне замовлення ресурсів — від 16 шт.

## Завершення івенту та нагороди

Івент завершується в одному з двох випадків:

- зомбі перемагають, якщо заразили всіх людей або адміністрація оголосила падіння бази;
- люди перемагають, якщо хоча б один виживший протримався 100 Minecraft-днів.

Якщо перемагають зомбі, усі активні учасники отримують по 120 талерів.

Якщо перемагають люди, талери отримують тільки виживші:

| Виживших людей | Нагорода кожному |
| --- | --- |
| 1 | 500 талерів |
| 2 | 440 талерів |
| 3 | 400 талерів |
| 4 | 360 талерів |
| 5 | 330 талерів |
| 6 | 300 талерів |
| 7 | 280 талерів |
| 8 | 260 талерів |
| 9 | 240 талерів |
| 10 | 220 талерів |
| 11 | 205 талерів |
| 12 | 190 талерів |
| 13 | 180 талерів |
| 14 | 170 талерів |
| 15 | 160 талерів |
| 16 | 150 талерів |
| 17 | 145 талерів |
| 18 | 140 талерів |
| 19 | 135 талерів |
| 20 | 130 талерів |
| 21 | 125 талерів |
| 22 | 120 талерів |
| 23 | 115 талерів |
| 24 | 110 талерів |
| 25 | 105 талерів |
| 26 | 100 талерів |
| 27 | 95 талерів |
| 28 | 90 талерів |
| 29 | 85 талерів |
| 30 | 80 талерів |

## Prisma

SQLite використовується для тесту:

```env
DATABASE_URL="file:./dev.db"
```

Для переходу на PostgreSQL змініть provider у `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

Після цього оновіть `DATABASE_URL`, запустіть міграції або `prisma db push`.

## Майбутня автоматична видача

Зараз видача ручна через адмін-панель і готові команди. Для RCON або окремого Minecraft-плагіна можна додати worker, який бере тільки замовлення зі статусом `pending`, виконує команди, а потім переводить їх у `issued`.
